language: java
dist: trusty
sudo: false

jdk:
  - oraclejdk8

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -DoutputDirectory="./" -B -V

script:
  - mvn sonar:sonar -DoutputDirectory="./"

addons:
  sonarcloud:
    organization: "utarwyn-github"
